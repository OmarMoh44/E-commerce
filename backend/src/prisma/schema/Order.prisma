enum Status {
    OnWay
    Delivered
}

model Order {
    id       Int       @id @default(autoincrement())
    amount   Int
    date     DateTime
    status   Status
    user_id  Int
    user     User      @relation(fields: [user_id], references: [id])
    products Product[]
}
